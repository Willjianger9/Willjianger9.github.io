document.addEventListener("DOMContentLoaded", function () {
  var typed = new Typed(".typing", {
    strings: ["Software Engineer", "Hockey Player", "Student", "Scienfitic Researcher"],
    typeSpeed: 120,
    backSpeed: 60,
    backDelay: 5000,  // 5 seconds before deleting
    loop: true,
  });
});
